package pdqsort

// Golang's slices.Sort method uses a pattern-defeating QuickSort (pdqsort) algorithm internally.
// pdqsort combines the fast average case of QuickSort with the fast worst case of heapsort.
func PDQSort(array []int) {

}
